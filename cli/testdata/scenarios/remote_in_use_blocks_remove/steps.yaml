steps:
  - name: "remote remove fails because base is imported by demo.default"
    args: ["remote", "remove", "base"]
    env:
      HOME: "__HOME__"
      EASYRICE_REPO: "__REPO__"
      EASYRICE_STATE: "__STATE__"
    expect:
      exit_code: 1
      stdout_contains:
        - "demo.default"
