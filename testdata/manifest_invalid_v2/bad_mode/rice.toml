schema_version = 1

[packages.badpkg]
description = "Package with invalid mode"
supported_os = ["linux", "darwin"]

[packages.badpkg.profiles.default]
sources = [{path = "config", mode = "hardlink", target = "$HOME/.config/badpkg"}]
